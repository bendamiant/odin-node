<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trainer List</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1> Trainer List</h1>
    <ul>
        <% trainers.map((trainer) => { %>
            <li>
                <div class="card">
                    <p><%= trainer.name %></p>
                    <a href="/trainers/<%= trainer.id %>"> Detail</a>
                    <form action="/trainers/<%= trainer.id %>" method="DELETE">
                        <button type="submit"> Delete</button>
                    </form>
                </div>
            </li>
        <% }); %>
    </ul>
</body>
</html>